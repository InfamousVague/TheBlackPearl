<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Not sandboxed: the torrent engine needs unrestricted inbound/outbound
	     network + to spawn ffmpeg, which the App Sandbox would block. -->
	<key>com.apple.security.app-sandbox</key>
	<false/>
	<!-- WKWebView's JS JIT needs writable-executable memory under hardened runtime. -->
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<!-- Allow loading the librqbit/axum dylibs Tauri links at runtime. -->
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
</dict>
</plist>
